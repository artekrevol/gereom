# Copy to .env and set your Railway Postgres URL (never commit .env)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Optional: comma-separated allowed origins for CORS if API is on another host
# CORS_ORIGINS=http://localhost:3000,https://yoursite.com
